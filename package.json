{
  "private": true,
  "workspaces": [
    "sdk"
  ],
  "scripts": {
    "generate-sdk": "yarn workspace sdk generate",
    "build-sdk": "yarn workspace sdk build",
    "build": "yarn generate-sdk && yarn build-sdk",
    "test": "yarn build && anchor test -- --features=testing",
    "pretest": "mkdir -p target/deploy && cp ./test-keypairs/test-program-keypair.json ./target/deploy/reclaim-keypair.json"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0"
  },
  "devDependencies": {
    "mocha": "10.2.0",
    "ts-node": "10.9.1",
    "ethers": "*",
    "@solana/spl-token": "*",
    "@solana/spl-memo": "^0.2.3",
    "@types/bn.js": "5.1.0",
    "@types/mocha": "10.0.1",
    "@types/node-fetch": "2.6.2",
    "typescript": "*",
    "prettier": "^2.6.2"
  },
  "resolutions": {
    "@solana/web3.js": "1.70.3",
    "@solana/spl-token": "0.3.6",
    "typescript": "4.9.4",
    "ethers": "6.11.1"
  }
}
